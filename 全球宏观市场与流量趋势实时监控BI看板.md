

# **全球宏观市场与流量趋势实时监控BI看板**

| 文档属性 | 内容 |
| :---- | :---- |
| **版本号** | **v1.1 (实时版)** |
| **变更点** | 纳斯达克/A股改为实时数据；Google 流量改为 5分钟级准实时 |
| **状态** | **待开发** |

---

## **1\. 数据源与采集频率变更 (核心变更)**

### **1.1 股市数据 (实时流)**

| 数据对象 | 变更后采集频率 | 建议接口/技术方案 | 关键逻辑说明 |
| :---- | :---- | :---- | :---- |
| **纳斯达克指数** | **实时 (Real-time)** | WebSocket (如 Alpaca, IEX Cloud) 或 高频轮询 (1秒/次) | 需区分**盘中** (9:30-16:00 EST) 与**盘后**状态。非交易时间显示最新收盘价。 |
| **A 股指数** | **实时 (Real-time)** | 新浪财经/腾讯财经 实时接口 (3秒/刷新) | 需处理午间休市 (11:30-13:00) 的数据静止状态。 |

### **1.2 流量数据 (准实时)**

| 数据对象 | 变更后采集频率 | 建议接口/技术方案 | 关键逻辑说明 |
| :---- | :---- | :---- | :---- |
| **Google 访问数据** | **5分钟/次 (5-min Interval)** | Google Analytics 4 (Realtime API) 或 自建流量埋点日志流 | Google Trends/SimilarWeb 公域通常不支持分钟级。**若必须公域数据**，需确认是否有高昂付费API支持，否则建议降级为“站内 Google 来源流量”或“Google Trends 关键词热度(部分支持小时级)”。 |

---

## **2\. 指标计算与存储逻辑修正**

由于引入了实时数据，指标定义需分为 **“静态历史”** 和 **“动态实时”** 两部分。

### **2.1 实时指标 (动态刷新)**

* **当前值 (Current Price/Visits)**：  
  * 股市：最新一笔成交价 (Last Price)。  
  * 流量：最近 5 分钟内的累计访问量 (Rolling 5-min) 或 瞬时并发数。  
* **实时涨跌幅 (Intraday Change %)**：  
  * 公式：$(\\text{当前价} \- \\text{昨日收盘价}) / \\text{昨日收盘价}$。  
  * *注：这比之前的“日环比”更适合实时看板，能反映当天的动态。*

### **2.2 历史指标 (T-7 趋势)**

* **7日趋势线**：  
  * 数据点需包含：前 6 天的**收盘价** \+ **今日实时价格** (动态跳动)。  
  * 计算 7 日平均值时，使用“今日实时价格”参与计算，导致均值也会随行情实时变动。

---

## **3\. 前端可视化需求更新**

**刷新机制要求**：

* **股市模块**：使用 **WebSocket** 或 **SSE (Server-Sent Events)** 推送，数字变化时需有 **红/绿 闪烁特效** (Flash effect) 提示用户数据已更新。  
* **Google 模块**：前端每 **5分钟** 自动请求一次最新数据，或通过倒计时进度条提示“下次更新时间”。

### **3.1 模块一 & 二：纳斯达克 / A股 (实时版)**

* **核心指标卡**：  
  * **数值**：精确到小数点后 2 位，实时跳动。  
  * **状态灯**：交易中 (Trading \- 亮绿灯) / 休市 (Closed \- 灰灯)。  
* **图表：分时走势 \+ 7日K线 (Mini Chart)**  
  * **主图**：保留原有的“7日趋势图”，但最后一个点必须是**实时跳动**的。  
  * *(可选新增)*：点击后弹窗展示“今日分时图”，展示 9:30 至今的分钟级走势。

### **3.2 模块三：Google 搜索流量 (5分钟级)**

* **核心指标卡**：  
  * **当前 5分钟访问量**：显示最近一个 5 分钟周期的数值。  
  * **趋势预测**：基于今日已过时间的流量，预测今日总量是否会超过昨日 (Run-rate Prediction)。  
* **图表**：  
  * **X轴**：粒度变更为 **“日期-时间”** (如 11-19 10:00, 11-19 10:05...)，展示今日的分钟级波动曲线。

---

## **4\. 风险与成本评估 (必须告知开发团队)**

1. **API 成本预警**：  
   * 公域的**实时**纳斯达克和 A 股接口通常是**收费**的 (如 Bloomberg, Wind, 或付费 API)。免费接口通常有 15 分钟延迟。  
   * **决策点**：请确认预算是否允许购买实时数据源？若不允许，需接受“15分钟延迟”的伪实时数据。  
2. **Google 数据源可行性**：  
   * **风险**：公域的 https://www.google.com/search?q=Google.com 全球实时访问量（分钟级）几乎是拿不到的（除了 Google 内部）。  
   * **调整建议**：建议将需求修正为 **“特定关键词在 Google Trends 的热度 (相对值)”** 或 **“自有产品从 Google 来源的实时流量”**。否则该指标无法落地。

---

## **5\. 修正后的交付建议**

**给开发的备注**：

“请注意，股市模块优先保障**WebSocket**连接的稳定性，断线需自动重连。Google 模块若无公域分钟级接口，请及时反馈，我们可能需要调整为‘小时级’刷新。”